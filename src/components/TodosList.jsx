import React from 'react';

const TodoList = ({ todos }) => {
    return (
        <div>
            <div>Name Description</div>
            {/* {todos.map((todo) => (
                <div>HOOO</div>
            ))} */}
        </div>
    );
};

export default TodoList;
